# RotationTimer

Rotation Timer is a simple watch app that sends a vibration through your watch at the set interval time and then repeats.

Simply set the desired time on the main screen using the +/- buttons and then press start.

The timer is formatted in MM:SS with the maximum interval being 59 minutes and 59 seconds.
